{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1",
    "typescript": "^4.0.2"
  },
  "scripts": {
    "build": "lerna exec --scope ds -- tsc",
    "dev": "cd packages/web && yarn dev"
  }
}